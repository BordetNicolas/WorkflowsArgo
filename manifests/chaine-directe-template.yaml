apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: chaine-directe
  namespace: workflows-argo
  labels:
    app.kubernetes.io/name: chaine_directe
spec:
  serviceAccountName: argo-workflow
  entrypoint: main
  artifactRepositoryRef:
    configMap: artifact-repositories
    key: default-v1
  arguments:
    parameters:
      - name: input_xml_key
        value: inbox/exemple.xml
        description: Clé S3 du XML source (ex. inbox/doc-001.xml)
      - name: packet_seed
        value: "42"
        description: Conservé pour compat (doc_count n'en dépend plus ; split 0-20 aléatoire)
      - name: simulate_missing_input
        value: "true"
        description: Conservé pour compat ; l'entrée XML est simulée (stub) à partir de la clé
      - name: enable_random_failures
        value: "true"
        description: Si true, chaque step peut échouer aléatoirement (pour tester les retries)
      - name: fail_rate_percent
        value: "30"
        description: Probabilité d'échec simulé par tentative (0-100)
  templates:
    - name: main
      dag:
        tasks:
          - name: validate-xml
            template: validate-xml
            arguments:
              parameters:
                - name: input_xml_key
                  value: "{{workflow.parameters.input_xml_key}}"
                - name: simulate_missing_input
                  value: "{{workflow.parameters.simulate_missing_input}}"
          - name: xml-to-ps
            template: xml-to-ps
            depends: validate-xml
            arguments:
              artifacts:
                - name: validated_xml
                  from: "{{tasks.validate-xml.outputs.artifacts.validated_xml}}"
          - name: modify-ps
            template: modify-ps
            depends: xml-to-ps
            arguments:
              artifacts:
                - name: document_ps
                  from: "{{tasks.xml-to-ps.outputs.artifacts.document_ps}}"
          - name: sort-split
            template: sort-split
            depends: modify-ps
            arguments:
              parameters:
                - name: packet_seed
                  value: "{{workflow.parameters.packet_seed}}"
              artifacts:
                - name: document_ps
                  from: "{{tasks.modify-ps.outputs.artifacts.document_ps}}"
          - name: archive-packet
            template: archive-packet
            depends: sort-split
            arguments:
              parameters:
                - name: packet
                  value: "{{item}}"
            withParam: "{{tasks.sort-split.outputs.parameters.archive_packets}}"
          - name: print-packet
            template: print-packet
            depends: sort-split
            arguments:
              parameters:
                - name: packet
                  value: "{{item}}"
            withParam: "{{tasks.sort-split.outputs.parameters.print_packets}}"
          - name: egate-packet
            template: egate-packet
            depends: sort-split
            arguments:
              parameters:
                - name: packet
                  value: "{{item}}"
            withParam: "{{tasks.sort-split.outputs.parameters.egate_packets}}"

    # 1 — Validation XML (mock). Entrée = paramètre clé ; artifacts inter-étapes via MinIO.
    - name: validate-xml
      retryStrategy:
        limit: "3"
        retryPolicy: Always
        backoff:
          duration: "2s"
          factor: 2
          maxDuration: "1m"
      inputs:
        parameters:
          - name: input_xml_key
          - name: simulate_missing_input
      outputs:
        artifacts:
          - name: validated_xml
            path: /work/validated.xml
      script:
        image: alpine:3.20
        command: [sh]
        source: |
          set -eu
          mkdir -p /work
          KEY="{{inputs.parameters.input_xml_key}}"
          SIM="{{inputs.parameters.simulate_missing_input}}"
          ENABLE="{{workflow.parameters.enable_random_failures}}"
          RATE="{{workflow.parameters.fail_rate_percent}}"
          echo "[validate-xml] clé logique S3=${KEY} simulate_missing_input=${SIM}"

          if [ "${ENABLE}" = "true" ]; then
            ROLL=$((RANDOM % 100))
            echo "[validate-xml][chaos] roll=${ROLL} fail_if_lt=${RATE}"
            if [ "${ROLL}" -lt "${RATE}" ]; then
              echo "[validate-xml][chaos] échec simulé (exit 42)" >&2
              exit 42
            fi
          fi

          if [ -z "${KEY}" ]; then
            echo "[validate-xml] ÉCHEC: input_xml_key vide" >&2
            exit 1
          fi

          cat > /work/validated.xml <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <documents source_key="${KEY}">
            <document id="1"><title>Stub document 1</title></document>
            <document id="2"><title>Stub document 2</title></document>
            <document id="3"><title>Stub document 3</title></document>
          </documents>
          EOF

          if ! grep -q '<documents' /work/validated.xml; then
            echo "[validate-xml] ÉCHEC: contenu non XML" >&2
            exit 1
          fi
          echo "[validate-xml] validation OK (XML stub pour ${KEY})"

    # 2 — XML → PostScript (document composé, mock)
    - name: xml-to-ps
      retryStrategy:
        limit: "3"
        retryPolicy: Always
        backoff:
          duration: "2s"
          factor: 2
          maxDuration: "1m"
      inputs:
        artifacts:
          - name: validated_xml
            path: /work/validated.xml
      outputs:
        artifacts:
          - name: document_ps
            path: /work/document.ps
      script:
        image: alpine:3.20
        command: [sh]
        source: |
          set -eu
          ENABLE="{{workflow.parameters.enable_random_failures}}"
          RATE="{{workflow.parameters.fail_rate_percent}}"
          echo "[xml-to-ps] transformation XML → PostScript (simulation)"
          if [ "${ENABLE}" = "true" ]; then
            ROLL=$((RANDOM % 100))
            echo "[xml-to-ps][chaos] roll=${ROLL} fail_if_lt=${RATE}"
            if [ "${ROLL}" -lt "${RATE}" ]; then
              echo "[xml-to-ps][chaos] échec simulé (exit 42)" >&2
              exit 42
            fi
          fi
          DOC_COUNT=$(grep -c '<document ' /work/validated.xml || true)
          cat > /work/document.ps <<EOF
          %!PS-Adobe-3.0
          % Simulated composed PostScript document
          % source: validated.xml
          % document_count: ${DOC_COUNT}
          /Courier findfont 12 scalefont setfont
          72 720 moveto
          (Chaine directe - document compose - ${DOC_COUNT} docs) show
          showpage
          EOF
          echo "[xml-to-ps] écrit /work/document.ps (${DOC_COUNT} documents)"

    # 3 — Modification du PostScript (mock)
    - name: modify-ps
      retryStrategy:
        limit: "3"
        retryPolicy: Always
        backoff:
          duration: "2s"
          factor: 2
          maxDuration: "1m"
      inputs:
        artifacts:
          - name: document_ps
            path: /work/document.ps
      outputs:
        artifacts:
          - name: document_ps
            path: /work/document_mod.ps
      script:
        image: alpine:3.20
        command: [sh]
        source: |
          set -eu
          ENABLE="{{workflow.parameters.enable_random_failures}}"
          RATE="{{workflow.parameters.fail_rate_percent}}"
          echo "[modify-ps] enrichment / modification PS (simulation)"
          if [ "${ENABLE}" = "true" ]; then
            ROLL=$((RANDOM % 100))
            echo "[modify-ps][chaos] roll=${ROLL} fail_if_lt=${RATE}"
            if [ "${ROLL}" -lt "${RATE}" ]; then
              echo "[modify-ps][chaos] échec simulé (exit 42)" >&2
              exit 42
            fi
          fi
          cp /work/document.ps /work/document_mod.ps
          cat >> /work/document_mod.ps <<'EOF'
          % --- modified ---
          % barcode overlay simulated
          % address block simulated
          EOF
          echo "[modify-ps] document_mod.ps prêt"

    # 4 — Tri / éclatement → listes JSON pour fan-out (0..N paquets / canal)
    - name: sort-split
      retryStrategy:
        limit: "3"
        retryPolicy: Always
        backoff:
          duration: "2s"
          factor: 2
          maxDuration: "1m"
      inputs:
        parameters:
          - name: packet_seed
        artifacts:
          - name: document_ps
            path: /work/document.ps
      outputs:
        parameters:
          - name: archive_packets
            valueFrom:
              path: /work/archive_packets.json
          - name: print_packets
            valueFrom:
              path: /work/print_packets.json
          - name: egate_packets
            valueFrom:
              path: /work/egate_packets.json
        artifacts:
          - name: packets_dir
            path: /work/packets
      script:
        image: alpine:3.20
        command: [sh]
        source: |
          set -eu
          ENABLE="{{workflow.parameters.enable_random_failures}}"
          RATE="{{workflow.parameters.fail_rate_percent}}"
          SEED="{{inputs.parameters.packet_seed}}"
          if [ "${ENABLE}" = "true" ]; then
            ROLL=$((RANDOM % 100))
            echo "[sort-split][chaos] roll=${ROLL} fail_if_lt=${RATE}"
            if [ "${ROLL}" -lt "${RATE}" ]; then
              echo "[sort-split][chaos] échec simulé (exit 42)" >&2
              exit 42
            fi
          fi
          # 0..20 paquets par canal (aléatoire) ; SEED reste dispo pour doc_count
          A=$((RANDOM % 21))
          P=$((RANDOM % 21))
          E=$((RANDOM % 21))
          echo "[sort-split] seed=${SEED} → archive=${A} print=${P} egate=${E} paquets (aléatoire 0-20)"

          mkdir -p /work/packets/archive /work/packets/print /work/packets/egate

          build_list() {
            CHANNEL="$1"
            COUNT="$2"
            OUT="$3"
            DIR="/work/packets/${CHANNEL}"
            echo -n '[' > "${OUT}"
            i=1
            while [ "${i}" -le "${COUNT}" ]; do
              PID="$(echo "${CHANNEL}" | cut -c1 | tr 'a-z' 'A-Z')-$(printf '%03d' "${i}")"
              DOC_COUNT=$((RANDOM % 5 + 1))
              S3_KEY="packets/${CHANNEL}/${PID}.ps"
              {
                echo "%!PS-Adobe-3.0"
                echo "% packet ${PID} channel=${CHANNEL} docs=${DOC_COUNT}"
                cat /work/document.ps
              } > "${DIR}/${PID}.ps"
              if [ "${i}" -gt 1 ]; then
                echo -n ',' >> "${OUT}"
              fi
              printf '{"packet_id":"%s","doc_count":%s,"s3_key":"%s","channel":"%s"}' \
                "${PID}" "${DOC_COUNT}" "${S3_KEY}" "${CHANNEL}" >> "${OUT}"
              i=$((i + 1))
            done
            echo ']' >> "${OUT}"
            echo "[sort-split] ${CHANNEL}: count=${COUNT}"
          }

          build_list archive "${A}" /work/archive_packets.json
          build_list print "${P}" /work/print_packets.json
          build_list egate "${E}" /work/egate_packets.json

    # Canal archivage — PS → PDF + XML (mock)
    - name: archive-packet
      retryStrategy:
        limit: "3"
        retryPolicy: Always
        backoff:
          duration: "2s"
          factor: 2
          maxDuration: "1m"
      inputs:
        parameters:
          - name: packet
      outputs:
        artifacts:
          - name: archive_result
            path: /work/out
      script:
        image: alpine:3.20
        command: [sh]
        source: |
          set -eu
          ENABLE="{{workflow.parameters.enable_random_failures}}"
          RATE="{{workflow.parameters.fail_rate_percent}}"
          PACKET='{{inputs.parameters.packet}}'
          echo "[archive] paquet=${PACKET}"
          if [ "${ENABLE}" = "true" ]; then
            ROLL=$((RANDOM % 100))
            echo "[archive][chaos] roll=${ROLL} fail_if_lt=${RATE}"
            if [ "${ROLL}" -lt "${RATE}" ]; then
              echo "[archive][chaos] échec simulé (exit 42)" >&2
              exit 42
            fi
          fi
          PID=$(echo "${PACKET}" | sed -n 's/.*"packet_id":"\([^"]*\)".*/\1/p')
          DOCS=$(echo "${PACKET}" | sed -n 's/.*"doc_count":\([0-9]*\).*/\1/p')
          KEY=$(echo "${PACKET}" | sed -n 's/.*"s3_key":"\([^"]*\)".*/\1/p')
          mkdir -p /work/out
          echo "%PDF-1.4 simulated archive of ${PID} (${DOCS} docs) from ${KEY}" > "/work/out/${PID}.pdf"
          cat > "/work/out/${PID}.xml" <<EOF
          <?xml version="1.0"?>
          <archive packet_id="${PID}" doc_count="${DOCS}" source_key="${KEY}"/>
          EOF
          echo "[archive] produits ${PID}.pdf + ${PID}.xml"

    # Canal impression — enrichissement PS + envoi printer (mock)
    - name: print-packet
      retryStrategy:
        limit: "3"
        retryPolicy: Always
        backoff:
          duration: "2s"
          factor: 2
          maxDuration: "1m"
      inputs:
        parameters:
          - name: packet
      outputs:
        artifacts:
          - name: print_result
            path: /work/out
      script:
        image: alpine:3.20
        command: [sh]
        source: |
          set -eu
          ENABLE="{{workflow.parameters.enable_random_failures}}"
          RATE="{{workflow.parameters.fail_rate_percent}}"
          PACKET='{{inputs.parameters.packet}}'
          echo "[print] paquet=${PACKET}"
          if [ "${ENABLE}" = "true" ]; then
            ROLL=$((RANDOM % 100))
            echo "[print][chaos] roll=${ROLL} fail_if_lt=${RATE}"
            if [ "${ROLL}" -lt "${RATE}" ]; then
              echo "[print][chaos] échec simulé (exit 42)" >&2
              exit 42
            fi
          fi
          PID=$(echo "${PACKET}" | sed -n 's/.*"packet_id":"\([^"]*\)".*/\1/p')
          DOCS=$(echo "${PACKET}" | sed -n 's/.*"doc_count":\([0-9]*\).*/\1/p')
          KEY=$(echo "${PACKET}" | sed -n 's/.*"s3_key":"\([^"]*\)".*/\1/p')
          mkdir -p /work/out
          cat > "/work/out/${PID}.enriched.ps" <<EOF
          %!PS-Adobe-3.0
          % enriched for physical printer
          % packet=${PID} docs=${DOCS} source=${KEY}
          % tray=1 duplex=true
          showpage
          EOF
          echo "[print] envoi simulé vers imprimante physique → ${PID}.enriched.ps"
          echo "SENT ${PID} to printer://physical/default" > "/work/out/${PID}.receipt.txt"

    # Canal guichet électronique — PS → XML + PDF embed (mock)
    - name: egate-packet
      retryStrategy:
        limit: "3"
        retryPolicy: Always
        backoff:
          duration: "2s"
          factor: 2
          maxDuration: "1m"
      inputs:
        parameters:
          - name: packet
      outputs:
        artifacts:
          - name: egate_result
            path: /work/out
      script:
        image: alpine:3.20
        command: [sh]
        source: |
          set -eu
          ENABLE="{{workflow.parameters.enable_random_failures}}"
          RATE="{{workflow.parameters.fail_rate_percent}}"
          PACKET='{{inputs.parameters.packet}}'
          echo "[egate] paquet=${PACKET}"
          if [ "${ENABLE}" = "true" ]; then
            ROLL=$((RANDOM % 100))
            echo "[egate][chaos] roll=${ROLL} fail_if_lt=${RATE}"
            if [ "${ROLL}" -lt "${RATE}" ]; then
              echo "[egate][chaos] échec simulé (exit 42)" >&2
              exit 42
            fi
          fi
          PID=$(echo "${PACKET}" | sed -n 's/.*"packet_id":"\([^"]*\)".*/\1/p')
          DOCS=$(echo "${PACKET}" | sed -n 's/.*"doc_count":\([0-9]*\).*/\1/p')
          KEY=$(echo "${PACKET}" | sed -n 's/.*"s3_key":"\([^"]*\)".*/\1/p')
          mkdir -p /work/out
          cat > "/work/out/${PID}.egate.xml" <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <egate_submission packet_id="${PID}" doc_count="${DOCS}" source_key="${KEY}">
            <embedded_pdf encoding="base64">JVBERi0xLjQgc2ltdWxhdGVkLVBERi1mb3ItJHtQSUR9Cg==</embedded_pdf>
          </egate_submission>
          EOF
          echo "[egate] XML guichet électronique écrit (${PID}.egate.xml)"
